Faker==22.7.0
pandas==2.2.0
SQLAlchemy==2.0.25 # now only for the notebooks. No need for the chatbot
langchain==0.1.11 # now only for the notebooks. No need for the chatbot
langchain-community==0.0.27 # now only for the notebooks. No need for the chatbot
langchain-core==0.1.35 # now only for the notebooks. No need for the chatbot
streamlit==1.42.0
boto3>=1.37.4
python-dotenv==1.0.1
sqlparse==0.5.0 # now only for the notebooks. No need for the chatbot
anthropic_bedrock
pydantic==1.10.13 # now only for the notebooks. No need for the chatbot
